#ifndef UTILS_H
#define UTILS_H


void clear_input_buffer();
void set_raw_input_mode();        // Function to get a character from the user in canonical mode;
void reset_input_mode();      // Function to reset the terminal setting to accept input in non-canonical mode;

#endif
